# Changelog

[English](CHANGELOG-en.md)

本项目的重要版本变更记录于此。

## 未发布

## v2.15.0 - 2026-09-08

本版本新增学习率曲线与矩阵结构两个训练前预览弹窗，并统一了三个预览弹窗的底座、尺寸与字号。

### 新增

- 新增学习率曲线预览：在 `lr_scheduler` 字段下方打开，按 sd-scripts 的调度公式绘制预热、衰减与重启曲线，支持悬浮读取任意步数的学习率；未读取到总步数时给出说明，对在训练中自行调整学习率的优化器不绘制误导性曲线。
- 新增矩阵结构预览（Anima 专属）：入口位于 `network_module` 字段下方和 LyCORIS 弹窗底部，按当前网络模块与算法绘制 LoRA / LoHa / LoKr 的权重分解示意，标出参数量与缩放系数。形状与参数量按 vendor 实现转写，factor 分解与 LyCORIS 逐例对拍、参数量与模块实例实测一致。

### 改进

- 统一 LyCORIS、时间步与学习率弹窗的底座：打开、关闭与焦点恢复走同一套封装，尺寸规则改用视口变量收口，补齐窄窗降级。
- 三个预览弹窗统一放大并提升字号（标签 11px、正文 12–13px），去掉参数标签的低对比度透明度；图表保持设计宽高比不拉伸，弹窗宽度按视口高度反推，避免高窗口拉长图表或矮窗口出现滚动条。
- 学习率预览的悬浮读数改为固定列宽的常驻单行展示，鼠标移出图表后保留最后读数。
- 修正时间步预览 y 轴标题与顶部刻度重叠，以及多项式衰减、预热与重启的边界计算。

### 文档

- README 新增「训练前预览」章节，介绍三个弹窗并配中英文截图；功能列表、项目结构、训练参数指南与程序参数表同步补齐过时内容，并精简重复表述。

### 测试

- 后端测试套件通过（448 passed，1,764 subtests passed）。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.14.0...v2.15.0)

## v2.14.0 - 2026-09-05

本版本精简 LyCORIS 算法选项，并修正归一化层文案与 Anima 调制层排除规则。

### 变更

- LyCORIS 算法选择器仅保留 LoCon、LoHa 和 LoKr，移除当前项目不支持或不稳定的 DyLoRA、GLoRA、Diag-OFT、Butterfly OFT、IA³ 与 Full fine-tuning 选项及其无效参数透传。
- 修正 LyCORIS `train_norm` 文案，明确当前支持 LayerNorm 与 GroupNorm。
- Anima 默认范围排除规则统一使用 `_modulation` 匹配，覆盖全部调制层，同时保持 AdaLN 豁免开关行为不变。

### 测试

- LyCORIS vendor contract、训练契约、LoRA+ 与实时交互回归测试通过（99 passed，1,581 subtests passed）。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.13.0...v2.14.0)

## v2.13.0 - 2026-09-05

本版本在 LyCORIS 面板上新增 Anima 训练范围细化开关，并同步 LyCORIS 上游 main 的修复。

### 新增

- 新增「对齐 sd-scripts 默认范围」与「训练 AdaLN 调制层」两个嵌套开关，仅在 LyCORIS 预设选择 attn-mlp 时显示，后者只有在前者开启后才出现。
- 默认范围与 sd-scripts 原生一致：只训练每个 Block 的注意力与 MLP，排除 AdaLN 调制分支、嵌入层、输出层和归一化层；「训练 AdaLN 调制层」把调制分支豁免加回，语义与主界面 AdaLN 开关一致。
- 排除规则通过上游预设文件机制生效：运行时把内置预设全集与 exclude_name 生成匿名预设文件，network_args 的 preset 指向该文件，vendor 零改动。

### 修复

- 同步 vendored LyCORIS 至上游 main：接通预设文件中的 exclude_name（上游 #288 修复），kohya/wrapper 两处模块遍历改用完整模块路径匹配；内核分发兼容 torch.compile（#289）；修复 DyLoRA 梯度路由、Full 算法首次前向崩溃与 LoKr 卷积旁路。
- 修复用户手写 exclude_name 与范围开关各自写一条被后写覆盖的问题：现在解析后并集合并为单条，避免静默丢失。
- lycoris_kernel_backend 不再写入导出 TOML（它是进程级环境变量，由启动时转发）。
- 参数预览的注释行不再使用斜体。

### 测试

- LyCORIS vendor contract、训练契约、train_adaln、代码质量契约测试全部通过（61 passed，1614 subtests）；真实 Anima 结构与 vendored create_network 全链路验证范围开关排除生效（默认 280 模块、豁免 AdaLN 后 448 模块）。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.12.1...v2.13.0)


## v2.12.1 - 2026-09-04

本版本修正 LyCORIS LoKr 的参数说明与 Full Matrix 提示，避免把 factor、dim/rank 和 Full Matrix 混为一谈。

### 修复

- 修正 LoKr Full Matrix、factor、dim/rank 与 alpha 的中英文说明，明确 Full Matrix 仍保留 Kronecker 结构且 factor 继续生效。
- 移除“阈值约为 16”和“alpha 与 rank 相同适用于所有模式”等容易误导的表述，改为说明自动切换条件取决于具体层尺寸和 factor。
- 移除前端按 `network_dim >= 16` 判断 Full Matrix 的静态近似，避免对不同尺寸的 Anima 层产生错误提示。

## v2.12.0 - 2026-09-04

本版本扩展 LyCORIS 训练支持，新增内核后端选择、LoRA+ 与 IA³ 配置能力，并完善自适应参数面板、配置校验和界面交互。

### 新增

- 新增 LyCORIS 内核后端选择（auto、Triton、TileLang、compile、Torch），启动训练前自动探测并在不可用时回退提示。
- 新增 LyCORIS 的 LoRA+ 支持、IA³ 算法与专用预设，以及 Full fine-tuning 算法选项。
- 新增 LyCORIS 自适应配置面板，按算法动态显示和校验参数，补齐 LoHa、LoKr、DyLoRA、GLoRA、OFT、IA³ 等配置。

### 改进

- 完善 LoRA+ 参数映射、适用范围和自动禁用规则，避免对不支持的 LyCORIS 算法静默生效。
- 优化 LyCORIS 参数层级、文案、图标和对齐；补充 conv、DoRA、OFT、LoKr 等参数的条件提示与约束。
- 同步 LyCORIS 上游内核实现，更新训练适配、配置迁移与兼容性处理。

### 测试

- 新增 LyCORIS vendor contract、LoRA+、实时训练状态等回归测试；项目测试集通过（449 passed，1790 subtests passed）。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.11.1...v2.12.0)

## v2.11.1 - 2026-09-03

本版本修复训练状态与仪表盘的错乱和残留问题，训练状态改为串行轮询单一来源；同时阻止自动推荐值覆盖手动配置，完善 LoRA-Muon 文档与界面文案，LyCORIS 参数不再渲染子组盒。

### 修复

- 修复训练任务状态与仪表盘生命周期：统一任务的完成、失败和终止状态机，终态任务不再污染实时仪表盘、不再重复发送终态事件；新训练启动时清理旧监控数据并切换实时任务订阅。
- 训练状态改为串行轮询单一来源：生命周期状态由 1.5 秒串行 HTTP 轮询唯一供给、唯一写入方绘制，侧栏与仪表盘同步更新；WebSocket 只保留进度、日志、指标、产物和硬件等流式数据。终止请求成功后立即对账一次，等待启动的任务也显示终止按钮；详情刷新不再依赖 WebSocket 存活，弱网隧道下可用。
- 修复自动值覆盖手动配置：LoRA-Muon 不再自动改写 `network_dim` 与 `network_alpha`；AdaFactor 与 split_attn 的推荐规则只在字段保持默认值时生效，手动输入不再被覆盖。
- 修复 TensorBoard 代理在浏览器刷新、切页或链路中断时输出未处理异常的问题，客户端断开以 HTTP 499 标记连接取消。
- LyCORIS 参数不再渲染子组盒，各参数按子项层级直接顺序渲染。

### LoRA-Muon 文案与推荐

- 训练文档的学习率说明按用法、原因、原理分层，补充白化与矩阵符号机制和调整后果；各参数文案去术语化，补齐与 Muon 的差异表和论文引用。
- 学习率字段的 LoRA-Muon 提示精简为两句：更新尺度与 AdamW 不同不能直接照搬，Anima 工程起点为 0.02；界面标签与描述移除多余括号。
- 移除 `inv_sqrt_steps` 在 Anima 上自动降为 5 的推荐，回归代码与论文默认的 7。

### 测试

- 清理 47 个低价值测试用例（一次性重构验证、动画时序细节、纯源码字符串断言等），其余测试全部通过，防回归能力不变。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.11.0...v2.11.1)

## v2.11.0 - 2026-09-01

本版本新增 LoRA-Muon 优化器，并补充对应的参数配置、界面提示、默认值和训练文档。

LoRA-Muon 目前属于测试优化器，主要用于 Anima LoRA 训练的实验性验证。不同模型、rank、硬件和训练配置下可能出现数值、收敛或速度方面的不稳定。后续版本仍可能调整算法实现、参数名、默认值、配置格式和训练行为，存在破坏性更新；重要训练请固定版本并保留训练配置。

### 新增 LoRA-Muon

- 新增 `vendor.lora_muon.LoRA_Muon`，参数通过 `optimizer_args` 传递。
- 支持 LoRA `down/up` 成对因子的 Gram 白化、矩阵符号更新，以及 Anima 使用的 Conv LoRA 参数形状。
- 新增动量、矩阵符号迭代、Gram 逆平方根迭代、数值保护和 gauge rebalance 等参数。
- Anima 界面的工程起始学习率为 `0.02`。它与 AdamW 的学习率不在同一数值尺度，不应直接把 `2e-5` 或 `1e-4` 等 AdamW 学习率照搬过来。
- 建议对 LoRA-Muon 单独测试多个学习率，例如从 `2e-5`、`1e-4`、`1e-3` 到 `2e-2`；论文中的 `0.1` 不代表 Anima 的固定推荐值。
- `network_dim` 与 `network_alpha` 不要求相等；界面对未手动修改的字段推荐从 `16/16` 开始。
- 优化器专属提示改为橙色附加说明，保留原有通用字段说明；gauge 相关参数在开关启用后显示。
- 补充参数范围校验、旧配置键迁移和 LoRA-Muon 专项测试。

### 实测资源与速度

以下数据来自本次真实 Anima 训练记录。步耗时采用完整训练迭代的日志区间，不列第 1 步初始化耗时，也不列稳态平均步耗时。

| 优化器 | 完整训练迭代耗时 | 峰值显存 allocated | 峰值显存 reserved |
| --- | ---: | ---: | ---: |
| LoRA-Muon | 约 1.0–1.1 秒/步 | 4715 MiB | 4872 MiB |
| AdamW | 约 0.7–0.9 秒/步 | 4802 MiB | 4950 MiB |
| AdamW8bit | 约 0.75–0.9 秒/步 | 4672 MiB | 4820 MiB |

完整 Anima LoRA 形状下的独立优化器状态显存：

| rank | LoRA-Muon | AdamW | AdamW8bit |
| ---: | ---: | ---: | ---: |
| 16 | 87.5 MiB | 175 MiB | 45.5 MiB |
| 32 | 175 MiB | 350 MiB | 90.0 MiB |

实际显存和训练速度会受到 GPU、rank、batch、分辨率、缓存和预览生成等因素影响，以上数据仅作为本次测试环境的参考。

### LoRA-RITE 元数据

- 统一 LoRA-RITE 的类名、注册 selector 与元数据名称为 `LoRA_RITE`，并同步前端映射、配置迁移与契约测试，避免元数据查看器截断。

### 文档与界面

- 补充 LoRA-Muon 参数说明、学习率尺度说明和多学习率测试建议。
- 补充 Anima 场景下的显存、计算量和 `max_grad_norm` 说明。
- 优化器下拉框增加 LoRA-Muon 的中性功能说明，不额外扩大限制性描述。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.10.0...v2.11.0)

## v2.10.0 - 2026-08-31

本次版本新增 ComfyUI 主题（复刻其默认深色界面），并把主题系统重构为每主题独立文件；同时修复一个会导致训练无法启动的配置写出缺陷——路径中含「反斜杠+x」序列（如 D:\datasets\xyz）时写出的 TOML 配置非法——以及若干主题相关的界面问题。

### ComfyUI 主题（新增）

- 新增 ComfyUI 主题，复刻其默认深色界面：配色、侧栏、Tab、弹窗与控件观感对齐 ComfyUI v1.49.6，附 Inter 字体。
- 页面切换与 Tab 面板动效对齐 ComfyUI 的 150ms 纯淡入；侧栏选中态改为静态平底样式，监控/环境 Tab 指示条滑动加快至 200ms。
- 主题切换器中的 ComfyUI 项使用官方前端的 C 形图标。

### 修复

- 修复路径中含「反斜杠+x」序列（如 D:\datasets\xyz）时写出的配置文件非法的问题：第三方 TOML 编码器会把路径中的反斜杠全部坍缩成单杠，训练启动即报解析错误。后端更换为自研 TOML 写入器，五条配置写出路径（Anima/SDXL/Krea 2 的训练与数据集配置）全部切换；同时修复纯数字目录名（如 "2024"）被写成 TOML 整数的问题。
- 修复浅色/深色主题下浏览器原生控件弹出层（如下拉列表）不跟随界面主题的问题。
- 修复深色/ComfyUI 主题下标签编辑器工具栏等处小下拉箭头图平铺成一栏小三角的问题。
- 修复参数字段的数值输入框比同排下拉框长出一截的问题，宽度已对齐。

### 重构

- 主题系统解耦：每个主题独立成文件（浅色/深色/ComfyUI），路由进入、弹窗浮现等展示型动效从全局样式收编进各主题文件，新增或调整主题不再需要改动全局样式。

## v2.9.5 - 2026-08-30

本次版本完善时间步分布预览的参数可见性：预览弹窗与文档页预览现在只显示真正参与塑造当前曲线的参数，缺失的成形参数卡补齐，并移除了一张与下拉框完全重复的卡片。

### 改进

- 时间步分布预览按采样方式条件显示生效参数：Sigmoid 缩放（sigmoid / shift / flux_shift）、Flow Shift（shift / sigma）、分辨率推导 Shift（flux_shift / krea2_shift）、Logit 均值与标准差（sigma + logit_normal）、Mode 缩放（sigma + mode）。文档页参数说明中的预览同步显示。
- 移除与「预览范围」下拉框内容完全重复的参数卡（弹窗与文档页各一处）。
- 参数卡的数值改为允许换行显示，不再出现省略号截断。

## v2.9.4 - 2026-08-30

本次版本统一全项目的文案双轨约定：控制台与 API 消息一律英文在前双语，前端界面词表补漏并清理历史遗留死键；同时修复官方底模被误拦、终止训练后立即重启时界面状态残留两个问题。

### 文案双轨统一

- 控制台与 API 消息统一为「英文 / 中文」英文在前格式，补齐此前纯中文、纯英文、中文在前的历史遗漏。
- 前端词表补齐 2 个缺失键（首页悬停提示、浏览按钮），修正 5 处翻译，删除 89 个零引用死键。
- 标签编辑器会话过期改为按错误码判断，替代原先按文案子串匹配的脆弱写法。
- 打标器逐张图片的进度行改为只写日志文件，不再刷控制台，任务汇总行保留。

### 修复

- 修复官方底模被误判拦截的问题：底模类型识别校验已移除，官方权重恢复可选。
- 修复终止训练后立即重启时，界面误显示「已终止」且需手动刷新才恢复的问题。

## v2.9.3 - 2026-08-29

本次版本为文本编码器（TE）磁盘缓存增加启动前一致性检查：修改 caption、调整 caption dropout 率这类不会让缓存自动失效的改动，现在会在启动训练前被检出并以弹窗提示重建，避免拿着旧缓存训练而不自知。同一版本完成了一轮死代码清理——前端样式表净删 651 行无效规则，后端移除不可达函数——并统一了弹窗实现、润色了两处参数文档。

### TE 缓存过期检查（新增）

- 启动训练前按引擎（Anima/SDXL）对比 TE 缓存 npz 快照与本次配置，检出三类问题：caption dropout 率与缓存快照不一致、caption 内容已修改、everyN_epochs 缓存路径静默无效。
- 检出问题时训练不启动，弹窗逐条列出警告并支持一键重建缓存；也可明确选择「按旧缓存继续」放行本次。
- 新增缓存清理端点，弹窗内重建与手动清理走同一条路径。

### 弹窗系统统一

- 两套确认弹窗合并为同一实现，替换残留的 3 处原生 alert/confirm（适配器警告、删除训练记录、清空日志），统一 Escape 与遮罩点击关闭行为。
- 弹窗样式还原为初始统一风格。

### 死代码清理

- 前端样式表净删 651 行：约 200 条死规则、146 个零引用类名，全部来自旧版仪表盘、旧版打标器工作台等历次重设计的遗留，顺带移除孤儿 `@keyframes`。
- 后端移除零引用函数 get_total_images。
- 清理经多重验证：静态引用保留性检查（在用类零误删）、九个页面状态的新旧样式逐元素等价对比（18 项布局与颜色属性零差异）、完整测试套件 420 项全部通过。

### 文档

- AdaLN 文档修正门控描述与 diffusion-pipe 对照，补充块外可训练模块说明。
- 时间步文档引用官方示例参数并润色中英文案与界面提示。

## v2.9.2 - 2026-08-27

本次版本把参数文档里的时间步分布预览和训练页弹窗统一成同一套双栏工作台布局，并同步了三个随功能演进失效的契约测试，完整测试套件全部通过。

### 分布预览对齐

- 参数文档的时间步预览改用与训练页相同的左右双栏工作台：左侧为预览范围下拉与六项参数读数（采样方式、采样偏移、中位时间步、Loss 权重、参考分辨率、预览范围），右侧为解析 PDF 图表，并补上与界面一致的 hover 读数栏、中位线与三区占比卡。
- 预览范围下拉在当前为 Anima/Krea 2 配置时可切换整体训练分布与各子集分布，与训练页行为一致；非 flow-matching 配置下显示 Anima 默认基准示例。

### 契约测试对齐

- 优化器元数据契约更新为当前 19 项清单与 6 组机制分组（baseline/stable/fast/longrun/autolr/matrix），Krea 2 菜单分组映射同步为当前 4 组。
- Tagger 标签编辑器的分隔条契约改为当前左右面板拖拽实现（tagger-single-divider，指针与左右方向键）。
- 完整测试套件 408 项全部通过。

## v2.9.1 - 2026-08-26

本次版本修复时间步分布预览重写后的文档与界面问题：参数文档现在复用训练器实际的解析 PDF 图表，图表在文档页可正常显示并支持悬停查看；同时修正概率密度文案、公式标记、hover 本地化和布局跳动。

### 时间步预览与文档

- 参数文档复用训练时间步预览的同一套解析 PDF 曲线、Loss 权重曲线和区域统计渲染器。
- 修复文档页图表宽度坍缩导致曲线不可见的问题，并让文档图表支持与参数页一致的 hover 检查。
- 将旧的 32 柱/固定随机模拟说明更新为 120 点确定性解析 PDF 实现。
- 修正概率密度术语与不支持的 `$f(t)$` 行内公式标记。
- 固定 density 检查栏布局，避免鼠标移动和离开图表时发生跳动；补充中英文本地化文案。

## v2.9.0 - 2026-08-26

本次版本的重点是参数页新增的「训练 AdaLN 调制层」开关：Anima 每个 DiT 块里按噪声水平实时调整特征缩放、平移与门控的调制层，此前被 sd-scripts 默认排除在 LoRA 之外，现在可以在参数页一键纳入训练，画风类数据集值得与关闭版本对照尝试。同一版本还把 LoRA+ 开关提升为平级控制，并在 AI 打标、内置文件选择器与后端骨架上做了多项整理与重构，项目许可证更换为 MIT。

### 训练 AdaLN 调制层（新增）

- 新增 Anima 专属开关，作用于 networks.lora_anima / loha / lokr。sd-scripts 创建 LoRA 网络时内置排除正则 `.*(_modulation|_norm|_embedder|final_layer).*`，调制层默认不参与训练；开启后训练器向 network_args 注入 include_patterns，把每个块的三个调制分支（对应自注意力 / 交叉注意力 / MLP）一并纳入，与自定义网络参数中手写的 include_patterns 自动并集合并，结果在 TOML 预览可见。
- 调制层决定每个去噪阶段特征的整体统计（色调、对比、通道能量）：不训练时这些保持底模原状，训练后 LoRA 额外获得调整全局特性的能力。官方 turbo 与 base 权重差的分析显示这条通路在蒸馏过程中变化显著，不过「画风 LoRA 训练调制层更好」目前没有公开的对照出图实验，建议与关闭版本同种子对比后择优。
- 调制层与主网络保持相同 rank 与 alpha，缩放一致；不提供调制层专属秩（上游不支持按模块单独设置 alpha）。同 rank 下文件约增大五成。
- 新增《AdaLN 调制层》中英双语文档页，参数页可直接跳转；产物可直接在 ComfyUI 加载，无需转换。

### LoRA+

- LoRA+ 开关从 network_module 的嵌套子项提升为平级开关，三个学习率倍率项作为其子项展开。

### AI 打标

- 新增自定义 API 模式，批量与单图双标签页：支持 OpenAI Chat Completions、Responses 与 Anthropic 三种协议，内置常用提供方预设与模型下拉框，并发数可调（1–8），错误逐条展示；API 密钥保存在浏览器本地，不会写入后端配置。

### 文件选择器

- 重做内置文件选择器：加大弹窗、子目录分组、双行信息（大小与修改时间）、训练目录附带图片与打标统计、键盘操作与名称筛选，筛选时底部显示命中数。
- 选择结果写回 ./models/… 风格的项目内相对路径（目录外仍为绝对路径），不解析符号链接。
- Windows 下启用 Per-Monitor V2 DPI 感知，修复高分屏模糊；无图形环境的 Linux（AutoDL 等）自动隐藏本地按钮，仅保留内置浏览器。

### 其他

- 重构后端骨架与打标器栈：模型识别改为 safetensors 头部键名精确匹配、依赖检测改用 packaging 版本比较、打标图片预处理改为纯 PIL 实现、批量打标跳过无关解码等；行为一致，性能与稳定性提升。
- 项目许可证更换为 MIT。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.8.0...v2.9.0)

## v2.8.0 - 2026-08-22

本次版本重做环境管理页：全页改为控制面板式流式布局——大字号行标题、状态与版本同句、次级说明独立成行，去掉表格列头的观感；运行环境与模型库改为页内 Tab 切换，附滑动指示条与淡入动效；组件与模型组默认收起，顶部新增「全部展开 / 全部收起」；分隔线不再穿过展开的行单元。模型库新增「附加模型」下载组，已收录 Any Anima (for LoRA training) v1.0.2。

### 环境管理页

- 顶部摘要统一呈现状态与进度：运行正常 / 需要注意 / 检测中三态提示，配合「加速 x/3 · 训练核心 x/3 · 模型库 x/y」文字计数，右侧常驻全部刷新。
- 运行环境与模型库拆为页内 Tab：滑动指示条跟随激活项（与监控台同款），面板切换播放淡入动画，Tab 选择记忆到本地。
- 组件行（Flash Attention、xformers、Triton、sd-scripts、LyCORIS、musubi-tuner）改为流式布局：首行名称 + 状态 + 版本，次行次级说明，末行主操作与「详情」文字链接；移除表格列头，展开状态用行内 caret 指示。
- 默认全部收起，仅安装/下载进行中的行自动展开以显示进度；「全部展开 / 全部收起」作用于当前 Tab 的行与模型组，重复点击目标状态不再重播动画。
- 分隔线修正：行头与展开体属于同一个行单元（两者之间不再划线），行与行之间保留细分隔线。

### 模型库

- 新增「附加模型」组：下载源指向 ame-la/train_use_models 仓库，已收录 Any Anima (for LoRA training) v1.0.2（Anima 底模），文件行附原始页面链接（Civitai）。
- 组件与文件行同为流式两行布局：文件名 + 大小 + 状态 + 操作 / 说明 + 来源 + 落地路径；组头显示 x/y 下载计数与合计大小。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.7.0...v2.8.0)

## v2.7.0 - 2026-08-22

本次版本为 Anima LoRA 训练新增四个优化器：Adan、AdEMAMix（含 8-bit 变体）与专为 LoRA 结构设计的 LoRA-RITE，专属参数全部接入训练表单并按 LoRA 场景调好默认值；优化器下拉框重新分组并补全逐条说明。

### 新增优化器

- Adan：在梯度统计之外跟踪相邻两步的梯度差分做前瞻更新，附带解耦权重衰减开关；默认学习率 1e-5（AdamW 基线的一半），实际步长与同学习率 AdamW 的对应关系写入说明。
- AdEMAMix 与 AdEMAMix8bit：快速（β1=0.9）与慢速（β3=0.9999）双时间尺度梯度记忆；混合强度 alpha 与两个缓升步数参数可直接调节，留空时按预估总步数自动填入。
- LoRA-RITE：消除同一 LoRA 不同等价写法导致的更新差异；自带按未放大梯度计算的裁剪（此时全局梯度裁剪阈值锁定为 0），eps 为"根 eps"语义并有专门提示。仅 Anima LoRA、仅标准 LoRA 结构，不能与 LoRA+ 同用，界面与文档均已标明。
- 四个优化器均通过 4 图 40 步真实训练冒烟验证；LoRA-RITE 的元数据名称写作 LoRARITE，避免元数据查看器按横线截断。

### 优化器下拉框

- 选项按技术特征重新分为六组：AdamW 及其存储变体、内置稳定化机制、前瞻差分与符号更新、双时间尺度梯度记忆、内部学习率估计与调度、矩阵结构更新；空组自动隐藏。
- 每个选项补上一句特点说明（更新机制、显存占用、学习率语义），只说事实不做推荐。

### 文档

- 优化器文档新增 Adan、AdEMAMix、LoRA-RITE 参数章节与梯度裁剪章节，学习率自动起始值表补充三个新优化器；LoRA+ 章节标明 LoRA-RITE 不兼容。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.6.0...v2.7.0)

## v2.6.0 - 2026-08-16

本次版本升级 Tagger 与标签编辑器：单图模式界面重新排版并新增「完成后卸载模型」开关；标签编辑器修复全量取图报错，并补齐一套完整的键盘操作流。

### Tagger 单图模式

- 右栏改为整栏滚动，分类不再各自内嵌滚动条；分类去卡片化并固定排序，总计标签去掉固定高度框，左列布局修复截断问题。
- 模型设置、输出选项、分类分组折叠展开补充动画。
- 单图模式的 CL 模型标签开关改为真正的 toggle，修复该模式下「添加模型标签」不生效的问题。
- 批量与单图模式新增「完成后卸载模型」开关：反推结束后将模型从显存卸载，关闭则保留以加速连续反推。
- 修复窄视口下分类阈值网格溢出（步进器被挤出边框）的问题；单图模式 WD 阈值改为步进器，与批量模式一致。

### 标签编辑器

- 修复全量取图时参数缺失导致的 TypeError：选择全部筛选结果、批量作用于筛选/全部、右键全局加删标签、标签云内联重命名全部恢复。
- 双击卡片改为查看大图；Enter 聚焦添加标签框，←/→ 翻图时选中卡片自动滚动入视图；Tab/Enter 焦点切换不再误中隐藏输入框。
- 路由离开守卫改用统一自定义确认弹窗（Esc 取消 / Enter 确认），与编辑器内其它确认一致。
- 撤销/重做/还原备份改用可区分的图标；标签云新增按频率/名称/长度排序切换；顶栏新增快捷键说明浮层。
- 草稿恢复后自动清除，不再重复提示；保存后提示编辑历史已归档至时间线。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.5.1...v2.6.0)

## v2.5.1 - 2026-08-15

本补丁修复 LoRA 训练表单时间步与加权分组中 weighting_scheme 的位置：logit_mean/logit_std/mode_scale 因前端父字段解析规则（show_if 数组取尾部键）被渲染到 weighting_scheme 之前，把它挤到了分组末尾；三个字段改为显式声明布局父字段，恢复与注册表一致的位置，Krea 2 训练档同样修复。新增回归测试用 node 执行真实渲染排序逻辑，锁定两个训练档的字段顺序。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.5.0...v2.5.1)

## v2.5.0 - 2026-08-15

本次版本重设计了环境管理页，并集中修复一批界面细节：环境页改为全宽三段式布局、进度刷新不再重建整页，就绪/失败/未配置状态一目了然；侧栏交互、监控台指示条、输出文件页等多处动画与布局问题一并解决。

### 环境管理页重设计

- 布局从卡片墙改为全宽三段式：Hero 总览 + 双列运行环境 + 全宽模型区，行式组件取代 details 卡片。
- 渲染改为分槽增量更新：进度刷新只更新对应区块，不再重建整页 DOM，折叠动画、输入焦点与日志滚动得以保留。
- 自定义折叠复用高度动画；busy/失败状态自动展开，面板展开状态持久化存储。
- 状态语义精简为四态：绿（就绪）/红（真实失败）/灰（未配置）/主题色（进行中），删除全部状态灯；未安装/未下载改为中性灰。
- 修复安装/下载失败原因被静默刷新吞掉、版本链接误触折叠、过期下载进度误报失败等问题；操作按钮统一右对齐，加载态合并进 Hero。
- 新增三个 node 单测，覆盖默认展开规则、失败原因保留与覆盖持久化迁移。

### 修复与打磨

- 侧栏交互质感：去掉激活态加粗与左侧蓝色竖线（改为图标着主题色），hover/按压不再绘制背景，全栏只保留滑动 pill 一个高亮，按压改为整体缩放回弹，消除文字闪烁。
- 修复 Tagger 下拉菜单错位/裁剪（入场动画填充由 both 改为 backwards）、监控台面板切换时滚动条引起的内容宽度跳动与闪现、输出文件吸顶表头透出缝隙等动画布局问题。
- 监控台 tab 指示条错位：run-detail 响应新增 output_count，输出文件计数首屏即有，按钮宽度不再后置变化；指示条改用 ResizeObserver 监听按钮尺寸，徽标出现/位数变化/语言切换时带动画平滑重定位。
- 输出文件页首次进入不再闪现：加载占位不播进入动画，真实文件列表首次落位时恢复浮入；退出历史记录时补齐计数清零，修复 tab 徽标计数残留。
- 分组导航轨道不再短暂出现在页面中间：rail 改用 x-teleport 挂载到 body，彻底脱离路由动画祖先。
- 修正训练表单说明文案的事实性错误（vae_disable_cache 速度方向、Krea 2 梯度检查点 CPU 卸载空操作、conv_dim/conv_alpha 留空行为、Prodigy 调度器例外等），补充 prodigy_d_coef、debiased_estimation_loss 等缺失字段说明，清理无引用死键，中英文案同步。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.4.0...v2.5.0)

## v2.4.0 - 2026-08-15

本次版本聚焦界面动效与导航体验：页面切换、标签切换和侧栏导航全部换上有层级感的平滑动画；训练监控台标签页改为常驻面板，切换不再重建内容，滚动位置与展开状态自动保留。

### 界面动效与导航

- 页面切换改为「旧页快速淡出 → 新页内容自上而下错峰浮现」，只作用于主内容区，侧栏与顶条不再跟随晃动；全程遵循系统「减弱动态效果」偏好。
- 侧栏新增滑动高亮指示条：点击导航项的瞬间高亮即平滑滑动到位，点击反馈不再滞后于页面挂载。
- 训练监控台四个标签页（概览/实时日志/样本/输出文件）改为常驻面板：切换只翻转可见性，日志增量渲染、样本滚动位置、输出文件展开状态在来回切换时完整保留；标签栏新增滑动指示条，面板内容自上而下分层浮现；切离样本页时自动取消后台媒体加载队列。
- Tagger 批量/单图模式切换补充分层进入动画，模式标签样式与监控台标签统一并复用滑动指示条；标签编辑器侧栏标签补充淡入过渡。
- LoRA 训练页的快速分组导航前置构建（仅依赖训练类型），进入页面时立即显示，不再等待表单挂载完成。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.3.8...v2.4.0)

## v2.3.8 - 2026-08-15

本补丁版本聚焦代码整洁与可靠性：合并后端重复实现、清理确认无引用的死代码，autosave 不再隐式依赖启动目录，并消除可变默认参数隐患；同时修正三篇参数文档的事实性错误、润色中英双语行文。

### 重构与清理

- Automagic 字段映射表统一收拢到 optimizer_contracts，adapter 与 validation 不再各自维护一份；tageditor 路由与 core 分别提取公共前置逻辑，monitor 集中定义 TensorBoard 标签常量与进度条参数。
- 删除前端零调用方法、config.js 旧 API 兼容分支，以及后端 python_bin、_MUSUBI_RUNTIME_PACKAGES 别名等确认无引用的死代码；training 路由 5 处 JSON 解析样板收敛为共享 _read_json_object，monitor 进度字段与 result.json 读取统一实现。
- autosave 改为由 constants.AUTOSAVE_DIR 定位输出目录，不再隐式依赖启动目录；postprocess_tags 可变默认参数改为 None 并在函数内归一化；清理 13 处无占位符 f-string。
- 新增 frontend/js/utils.js 收拢跨模块共享的 esc、tMonitor 与训练分组映射；测试工厂收敛至 tests/helpers.py；.gitignore 裸 config 规则改为白名单写法。

### 文档

- 修正 lora-plus、optimizers、timesteps 三篇参数文档的事实性错误（Krea 2 兼容性、LoRA+ 适用列表、Muon 适用范围、percentile_clipping 参数名、musubi-tuner 源码路径归属等），去除翻译腔并润色中英双语行文。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.3.7...v2.3.8)

## v2.3.7 - 2026-08-13

本补丁版本聚焦前端性能与体验：静态资源启用 gzip 压缩与版本化缓存，冷启动与页面刷新传输量大幅下降；并修复首页版本徽章过期、README 命令路径损坏等问题。

### 性能与传输

- 所有静态资源与 API 响应启用 gzip 压缩：首次加载传输量约 1.3 MB → 约 280 KB（局域网或慢链接场景收益最大）。
- 版本化静态资源（?v=）改为一年期 immutable 缓存，未版本化资源保持回源校验；页面刷新时静态请求从 21 次降到 1 次（仅 index.html）。
- /api/fields 增加 ETag 与 304 响应，字段注册表未变化时不再每次重复下载约 88 KB。
- i18n 启动只同步加载当前语言（约 96 KB），另一语言后台预载，切换语言行为不变。
- 弱网模式（缩略图串行加载）改为按连接质量自适应默认：本机与局域网默认并行加载，2g/3g/省流量模式下自动开启，也可随时手动切换。
- 延迟加载 pandas/numpy/cv2，GUI 冷启动提速约 31%。

### 修复与整理

- 首页 Release 徽章改用后端真实版本号（此前显示过期的 v1.3.3）。
- `<html lang>` 随启动语言同步，屏幕阅读器与浏览器翻译行为正确。
- 修复 README 中损坏的 venv 路径（控制字符污染导致 Windows 命令不可复制）。
- 修正 README 拼写错误并补充测试依赖与 Linux 命令说明。
- 清理已确认的死代码（未使用的函数、导入、常量与 Chart.js 静态文件）。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.3.6...v2.3.7)

## v2.3.6 - 2026-08-12

本补丁版本以可移植的训练配置 YAML 取代旧预设管理，并整理训练表单层级、参数预览和监控细节，使配置保存、恢复与实际训练行为更加一致。

### 训练配置 YAML

- 新增版本化训练 YAML 的下载、导入和运行目录自动保存，完整保留当前生效的表单值、条件参数和预览设置，避免应用默认值变化后旧配置悄然漂移。
- 训练历史优先从 `training.yaml` 恢复完整表单，同时继续兼容旧版扁平 TOML 导入。
- 从用户 YAML 中移除可由训练配置档推导的 `engine_id` 与 `adapter_id`；内部运行记录仍保留核心信息用于调度与诊断。
- 移除旧训练预设管理界面、接口和本地状态，统一为下载与导入配置文件的工作流。

### 表单与参数预览

- 精简训练表单的重复容器、说明和旧交互，将条件参数按依赖关系稳定排列并保留清晰缩进。
- 将优化器选择器移至学习率与调度参数之前，优化器专属参数仍保留在通用参数下方。
- 侧边 TOML 预览继续忠实展示实际运行参数，并弱化显示应用默认但必须显式传递的值，复制内容不受影响。

### 修复与整理

- 修复参数说明中的时间步分布图表，并统一训练监控日志计数与进度状态更新。
- 精简环境加载提示，清理预设移除后的前端残留调用、重复常量和无效样式。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.3.5...v2.3.6)

## v2.3.5 - 2026-08-10

本版本为 Anima LoRA 引入 PyTorch 原生 Muon 优化器，并补齐从界面配置、参数校验到 sd-scripts 启动和 CUDA 更新的完整链路；同时统一训练产物、Tagger 与标签编辑器的图片预览能力。

### Muon 优化器

- 在 Anima LoRA 优化器菜单中加入原生 `torch.optim.Muon`，SDXL 配置中不显示该选项；sd-scripts 继续使用原生选择器 `Muon`，无需额外包装器。
- 提供学习率缩放、动量、Nesterov、Newton-Schulz 迭代次数与系数、`eps` 和权重衰减配置；默认使用 `match_rms_adamw`，Anima 学习率起点为 `2e-5`。
- 将产品默认 `weight_decay=0` 明确传给 sd-scripts，覆盖 PyTorch Muon 的库默认 `0.1`，避免界面值与实际训练参数不一致。
- 增加参数范围、Anima 限定、前端显示和配置适配测试；使用项目生成的完整参数创建原生 Muon，并通过 CUDA `backward()` 与 `step()` 验证实际更新。
- 扩充中英文优化器文档，说明 Muon 与 AdamW 的更新差异、参数影响、计算开销和对照实验方式，并优化窄文档栏中的参数排版。

### 图片预览

- 新增统一图片预览接口，按数据集、训练产物和 Tagger 场景生成 WebP 缩略图、预览图与检查图，同时保留原图直出。
- 标签编辑器增加大图灯箱、缩放拖动和右键查看原图，并移除旧图片路由与重复缓存实现。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.3.4...v2.3.5)

## v2.3.4 - 2026-08-09

本补丁版本修复训练任务创建成功后，训练页、仪表盘与侧栏状态未能即时同步的问题。

### 训练状态同步

- 训练启动接口返回成功后立即更新共享训练状态与任务标识，不再依赖下一条 WebSocket 事件才显示训练中。
- 主动获取一次新的实时快照，校准后端任务状态；在 SSH 端口映射、代理或网络延迟场景下无需刷新页面即可看到新任务。
- 避免启动期间已有实时快照请求覆盖新任务状态，并增加对应的前端状态与竞态回归测试。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.3.3...v2.3.4)

## v2.3.3 - 2026-08-08

本补丁版本统一训练器主要页面的布局与交互语言，改进 Tagger 单图检查流程，并修复启动输出、训练监控和标签编辑器中的细节问题。

### 启动与界面一致性

- 优化 Windows 与 Linux 启动进度和就绪信息；窄控制台中的服务地址、TensorBoard 与日志路径可以自然换行，不再被固定边框挤压。
- 统一仪表盘、Tagger 与设置页的页面外壳、工具栏高度、控件样式、字体和滚动行为，减少标签切换时的像素偏移与视觉割裂。
- 修复训练历史“运行中”状态的本地化显示，并将中文训练摘要中的 `Epoch` 统一为“轮次”。
- 修复标签编辑器在未选择数据集目录时顶部加载进度不会结束的问题。

### Tagger 单图工作区

- 图片预览支持滚轮缩放、拖拽平移，以及双击或工具栏按钮恢复适应窗口；移除无实际帮助的预览背景切换。
- “总计标签”区域支持鼠标拖拽和键盘方向键调整高度，并根据左栏剩余空间限制可用范围。
- 置信度分类的箭头、名称和数量区域改为整行可点击的展开/收起按钮；显示复选框与复制按钮仍保持独立操作。

### 时间步参数与文档

- 将“子集采样偏移”移动到 `weighting_scheme` 及其条件参数之后，并增加直达对应章节的“查看说明”入口。
- 明确子集 offset 仅在 `sigmoid`、`shift`、`flux_shift` 中生效；`sigma` 下的 `logit_normal` 与 `mode` 改变所有子集共享的基础分布，不会修改单个子集偏移。
- 同步完善中英文时间步文档、参数提示和回归测试。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.3.2...v2.3.3)

## v2.3.2 - 2026-08-08

本补丁版本修复训练监控的日志与预览样本行为，并整理 SDXL 训练目标参数的显示层级。

### 训练监控

- 预览样本优先按训练器文件名中的生成时间、训练进度和提示词序号排序；复制或恢复输出目录导致文件修改时间变化时，最新样本和训练顺序仍保持正确。
- 修复完整日志首次进入时尾部请求参数无效的问题，并在训练任务或日志来源切换时立即清除旧的加载状态。
- 样本页切换“训练顺序”与“最新优先”时原地重排既有预览节点，不再重建整个样本网格，避免闪烁和短暂破图。
- 总览页移除重复的“最新样本”卡，将空间用于扩展训练诊断区域。

### SDXL 参数表单

- 将 `zero_terminal_snr` 紧邻 `v_parameterization` 展示；它仍仅在启用 v 参数化时可见，但不再显示为缩进子项。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.3.1...v2.3.2)

## v2.3.1 - 2026-08-08

本版本为文档更新：时间步参数指南新增「按子集设置时间步偏移」章节，并修正表述与参考链接。

### 参数文档

- 时间步参数指南（`docs/parameters/timesteps*.md`）新增「按子集设置时间步偏移」章节：说明数字前缀子集的识别规则、`subset_timestep_offsets` 的映射形式与 `dataset.toml` 转换、偏移的计算公式与生效模式，并给出推荐范围与对照实验建议。
- 同步更新参数生效关系矩阵与常见误区条目；修正中英文表达，并将依据与参考资料固定到本次核查的提交（2026-08-08）。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.3.0...v2.3.1)

## v2.3.0 - 2026-08-07

本版本重点加入按训练子集配置时间步采样偏移的完整工作流，并让偏移前后的训练关注区域可以在界面中直接比较；同时补齐正则化数据集和可复核的训练步数计算。

### 子集时间步采样偏移

- 在现有时间步参数下方加入子集编辑器，可按 `10_face`、`3_full_body` 等数字前缀训练目录分别设置 `timestep_sampling.offset`；支持键盘小数输入、步进按钮、撤销和重置。
- 训练启动时生成完整的 sd-scripts `dataset.toml` 并通过 `--dataset_config` 接入真实训练链路；空值与零值不写入，已删除的子集会被拒绝，正则化子集始终保持无偏移。
- 时间步图新增基础分布、整体训练分布和单个子集范围，可并列查看偏移前后的柱形分布、中位 timestep 变化以及低、中、高噪声区占比。
- 明确 offset 加在 sigmoid 前的正态采样值上：负值偏向低噪声细节，正值偏向高噪声结构；仅在 `sigmoid`、`shift` 和 `flux_shift` 模式生效。

### 数据集与训练步数

- Anima 训练表单支持启用正则化数据、配置正则目录与先验损失权重，并在步数估算中区分训练子集和正则子集。
- 步数估算对齐 sd-scripts 的目录注册、分桶与向上取整规则，并展示可逐步核对的完整计算过程。

### 上游同步

- 同步 vendored `sd-scripts` 至 `37a1cbb`（`v0.11.1-30-g37a1cbb`）：新增 per-subset `timestep_sampling.offset` 与 `--show_timesteps_offset` 预览参数，更新 IPEX 库并修复拼写。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.2.4...v2.3.0)

## v2.2.4 - 2026-08-05

本补丁版本完善训练参数指南：新增常见问题与统一证据格式，并统一术语。

### 参数文档

- LoRA+ 与优化器指南新增“常见问题”章节，覆盖倍率选择、组合被禁用、推荐值被替换等高频问题。
- 三份指南统一证据规范：事实核查日期与固定提交链接；时间步指南新增“依据与参考资料”章节。
- 统一术语（检查点、随机种子、重复次数），修正英文版 Lion 参考链接损坏的提交哈希。
- 中英文 README 新增“训练参数指南”入口。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.2.3...v2.2.4)

## v2.2.3 - 2026-08-03

本补丁版本恢复更自然、面向实际训练的时间步、LoRA+ 与优化器参数指南，并精简会锁死文案、样式和内部实现的脆弱测试。

### 参数文档

- 恢复时间步与 LoRA+ 的中英文实用指南，保留训练参数页面到对应章节的链接。
- 将优化器中英文指南恢复到重写前的可读版本，继续覆盖当前支持的优化器和参数设置。

### 测试维护

- 删除固定文案、来源提交、CSS 布局和源码字符串快照等低价值断言。
- 保留文档路由、训练字段锚点、配置生成、训练适配和运行时行为测试。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.2.2...v2.2.3)

## v2.2.2 - 2026-08-03

本补丁版本统一 LoRA 参数的界面、配置适配与校验契约，修订双语说明和内置参数文档，并补充可复核的固定上游依据。

### 参数契约与表单

- 统一 Automagic3 缺失 `max_lr` 时的有效默认值为 `1e3`，同时保留旧预设和 API 请求中显式提供的值。
- 修正 LoRA+、AdaFactor、Prodigy 系列、卸载方式、scheduler 周期和非正方形分辨率预览的联动说明与行为边界。
- 清理中英文表单中的速记表达、术语大小写和重复括注，使标签、提示和锁定原因各自表达单一职责。

### 文档与来源

- 更新时间步、LoRA+ 和优化器指南，区分上游机制、当前训练器行为与配置值，并增加固定提交版本的依据与核验日期。
- 修正 Automagic3 和 EmoSens 的来源归属：二者为上游优化器集成，当前训练器负责 sd-scripts 适配、运行时接入和兼容约束。

### 回归保护

- 增加 Automagic3 默认值、LoRA+ 限制、卸载组合、scheduler 说明、分辨率预览和固定上游链接的自动化测试。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.2.1...v2.2.2)

## v2.2.1 - 2026-08-02

本补丁版本重构标签编辑器的数据与会话工作流，并集中改善训练仪表盘在大量样本、日志和输出文件下的桌面使用体验。

### 标签编辑器

- 重构标签编辑器的仓储、会话、快照和时间线实现，收拢前后端状态契约并扩充回归测试。
- 优化图片网格卡片的自适应布局，使不同桌面宽度下的图片和编辑信息保持稳定可读。

### 训练仪表盘

- 修复样本卡片跨行重叠和文件名消失，使用稳定响应式网格完整展示图片，并增加训练顺序与最新优先切换。
- 合并 HTTP 快照与 WebSocket replay 的精确重叠，折叠同一步骤的相邻 `tqdm` 更新，并统一完整日志、分页和实时模式的展示行。
- 为输出文件增加搜索、类型筛选、模型与其他文件独立排序、可见结果全选和批量操作，同时提升表格字号、行高和窄窗口滚动体验。
- 统一仪表盘四个 Tab、诊断信息、状态徽标和辅助数据的桌面字号层级，并补齐中英文界面文案。

### 回归保护

- 增加日志重叠、同一步进度替换、分页总数、样本排序及输出文件筛选排序的自动化测试。
- 完成常规与窄桌面浏览器验证，确保样本卡片不重叠、输出表格局部滚动且页面无横向溢出。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.2.0...v2.2.1)

## v2.2.0 - 2026-08-01

本版本统一桌面工作台与启动信息体验，增强优化器元数据和 Anima 学习率联动，并继续完善 Tagger 界面与训练文档。

### 桌面工作台与启动体验

- 重构应用启动、日志初始化和服务信息输出，集中展示访问地址、运行环境与启动状态，并改善异常回退行为。
- 统一训练页、Tagger 与通用控件的桌面布局和尺寸，精简重复样式，提升窄窗口下的信息密度与可读性。
- 调整 Tagger 单图工作区、模型配置和结果展示，使分类阈值、角色标签与编辑操作更清晰。

### Anima 学习率联动与兼容性

- 将所有 `setIfDefault` 联动改为基于字段来源判断，保护手动输入、导入配置、预设和旧版本地草稿，即使数值恰好等于默认值也不再静默覆盖。
- 按训练页面和配置类型分别持久化字段来源；单字段复位重新启用当前依赖条件下的自动推荐，全量复位恢复该配置类型的完整默认组合。
- Anima 学习率输入框的占位提示改为直接读取字段注册表中的 `autoValue` 规则，删除前端重复维护的推荐值映射。

### 优化器与训练配置

- 新增集中式优化器元数据，补全参数默认值、适用范围和序列化契约，并优化 Anima 优化器学习率与调度器默认组合。
- 重构 Krea 2 配置适配和训练字段注册逻辑，减少前后端重复定义并扩充回归测试覆盖。
- 修复预设应用撤销和字段来源状态恢复，避免自动推荐错误覆盖用户配置。

### 文档

- 更正 `cosine_with_restarts`、Anima rank/alpha、Schedule-Free warmup 和 Lion weight decay 的依据边界，并将外部依据固定到 2026-07-31 核查的提交。
- 精简重复学习率数值，以优化器文档中的学习率主表作为详细推荐矩阵。
- 全面更新中英文 README、参数说明与界面文案，使训练后端、安装流程和运行边界保持一致。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.1.0...v2.2.0)

## v2.1.0 - 2026-07-30

本版本重构 Tagger 工作流，并系统整理训练参数说明、优化器文档和 Krea 2 高级配置。

### Tagger 工作台

- 将 Tagger 重构为独立工作台，统一单图检查、批量处理、结果编辑和标签文件写入流程。
- 支持 WD EVA02 Large v3、WD ViT Large v3、CL Tagger v1.02 和 Camie Tagger v2，并按模型能力显示分类阈值与角色标签控制。
- 改进模型选择、单图布局、置信度结果和标签格式化，修复切换模型后状态不同步与名称显示错误。
- 移除未形成稳定工作流的本地 LLM 标签反推路径，保留可预测的 ONNX Tagger 运行边界。

### 训练参数与优化器

- 全面审查 SDXL、Anima 与 Krea 2 可见字段，将简短中文标题与默认值、适用条件、联动关系和副作用说明分离。
- 校正 Batch、梯度累积、Dropout、时间步采样、Loss 权重、CAME、AdaFactor、Schedule-Free 和十种学习率调度器的技术语义。
- 新增中英文优化器参数文档，并补全 StableAdamW 的权重衰减、Kahan 求和与参数序列化支持。
- 为 Krea 2 时间步、注意力实现和优化器选项补充下拉说明，修正 Lion8bit、缓存指纹和 RAW/Turbo DiT 的说明。

### Krea 2 FP8 与兼容性

- 将 `fp8_base` 与不可独立选择的 `fp8_scaled` 合并为一个动态缩放 FP8 开关，开启时仍输出两个 musubi 参数。
- 继续接受旧预设和 API payload 中的 `fp8_scaled`，并在校验阶段按 `fp8_base` 自动归一化。
- 为字段 schema fallback 增加缓存版本，避免升级后浏览器继续显示旧标题或遗漏新 hint。

### 回归保护

- 新增 FP8 合并、旧 payload、调度器说明、双语 i18n、可见标题与字段缓存版本契约测试。
- 完整单元测试覆盖 Tagger 工作台、多训练核心、字段 schema、优化器参数和 Krea 2 配置生成。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.0.1...v2.1.0)

## v2.0.1 - 2026-07-26

本补丁版本集中修复 v2.0.0 发布后发现的训练监控、优化器联动、依赖下载和文档导航问题。

### 训练日志与实时监控

- 将网页训练日志中的终端宽度 `tqdm` 进度条压缩为稳定宽度，避免超长空白撑开日志区域并导致指标换行。
- 统一日志解析、TensorBoard 增量和前端回退路径的学习率科学计数法，消除 `e-5` 与 `e-05` 等格式跳变。
- 屏蔽 TensorBoard 反向代理产生的常规 `httpx` 请求日志，防止 Rich 动态进度条被反复固化到控制台。

### 优化器配置

- 修复优化器切换时 `weight_decay`、`max_grad_norm`、`betas`、`eps` 等联动字段重置到错误默认值的问题。
- 将 Anima 与 SDXL 的 `AdamWScheduleFree` 推荐学习率从通用库默认值 `0.0025` 调整为更适合 LoRA 训练的 `3e-4`。
- 同步前端离线字段配置与优化器契约测试，用户手动设置的学习率仍会保留。

### 安装与文档体验

- 缩短 Flash Attention 单下载源连接超时，取消同源重复重试，并分离 API 与 wheel 下载代理以改善自动回退。
- 修复内置文档短章节滚动时目录高亮提前跳到后续章节的问题。
- 补齐并对齐 v2.0.0 中英文更新日志，保留完整版本历史与语言切换入口。

### 回归保护

- 新增优化器联动重置、Flash Attention 下载回退、训练日志清理、实时学习率格式、控制台日志级别和文档目录定位测试。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v2.0.0...v2.0.1)

## v2.0.0 - 2026-07-25

这是一次训练架构级更新。v2.0.0 引入多训练核心体系，正式接入 Krea 2 RAW DiT LoRA，并将默认训练栈升级到 PyTorch 2.10.0 + CUDA 13.0。训练配置、缓存预检、环境管理、参数预览和内置文档也围绕多核心工作流进行了系统整理。

### 多训练核心架构

- 新增显式训练核心注册表，隔离不同训练器的字段、参数验证、命令生成和启动流程。
- `sd-scripts` 继续负责 SDXL 与 Anima，`musubi-tuner` 负责 Krea 2 RAW DiT LoRA。
- LyCORIS 继续作为可选适配器核心，通过 `lycoris.kohya` 接入现有训练流程。
- 前端会根据当前训练类型切换对应字段、预设、优化器和时间步选项，避免不同核心的参数互相污染。
- TOML 导入导出、参数预览、训练预检和任务监控均已适配核心切换。

### Krea 2 RAW DiT LoRA

- 完整接入 Krea 2 模型、VAE、Qwen3-VL 文本编码器和数据集 TOML 配置。
- 支持潜空间缓存与文本编码器缓存，并在训练前检查缓存是否完整、是否仍与图片、标注和模型匹配。
- 自动归集 Krea 2 数据集缓存，减少手动维护缓存路径和中间文件的成本。
- 支持训练命令生成、进度估算、日志监控、停止训练和中断恢复。
- 增加 Krea 2 专用优化器、调度器、时间步采样和网络参数选项。
- 修复 Krea 2 参数预览高亮、预设导出、字段命名和自定义优化器注入边界。

### CUDA 13 共享训练环境

- 默认训练环境从 PyTorch 2.10.0 + cu128 升级到 PyTorch 2.10.0 + cu130。
- RTX 30、40、50 系列使用统一的 CUDA 13.0 训练栈。
- 已有 cu128 `venv` 会在下次启动时自动迁移，新安装直接使用 cu130。
- 已安装的 xformers、FlashAttention、Triton 和 bitsandbytes 会按新环境重新匹配。
- ONNX Runtime GPU 会切换到 CUDA 13 对应版本，同时修复同次启动中依赖版本缓存未刷新造成的误卸载。
- 无 NVIDIA 显卡的机器仍可完成环境安装并运行 GUI，仅训练功能需要 NVIDIA GPU。

### Krea 2 运行时管理

- Krea 2 与主应用共用项目根目录的 `venv`，不再维护独立核心虚拟环境。
- 通过 `requirements-musubi-krea2.txt` 收敛 Krea 2 所需的共享依赖版本。
- 日常启动只执行快速元数据检查，版本正确时不会重复运行 pip、卸载依赖或导入完整训练栈。
- 完成依赖同步后以及实际执行 Krea 2 任务前，仍会运行完整导入验证。
- 旧版遗留的 `venv/cores/musubi` 不再读取、写入或自动删除，可在确认新环境正常后手动清理。

### 参数配置与预览

- 增强 TOML 与训练命令预览，可从表单变更定位对应参数。
- 改善多选下拉菜单、预设加载、核心切换和跨字段自动值同步。
- 修复 Anima 与 Krea 2 时间步采样选项不一致的问题。
- 新增 Anima 与 Krea 2 时间步分布预览，提供直方图、密度、累积分布和信噪比等视图。
- 完善 LoRA+ 参数联动、优化器兼容约束和界面提示。

### 内置训练指南

- 新增中英双语时间步完整指南，覆盖常见采样方式、参数含义、分布特征和适用场景。
- 重写 LoRA+ 指南，补充学习率比例、优化器兼容性和配置建议。
- 文档支持目录、锚点导航、公式排版和参数界面的就地入口。
- 优化桌面与窄屏布局，长公式、表格和导航在较小窗口下也可正常阅读。

### 启动与环境管理体验

- Windows 与 Linux 启动器增加即时阶段提示和动态进度展示。
- 精简正常启动输出，同时保留可定位安装与依赖问题的诊断日志。
- 环境管理页面改为渐进加载，减少首次打开时的等待和无响应感。
- 优化 Krea 2 运行时探测，健康环境不会在每次启动时重复执行昂贵检查。

### 上游同步

- 同步 vendored `sd-scripts` 至 `6565877`（`v0.11.1-9-g6565877`）。
- 补充 Anima aesthetics 权重键名兼容，并修复自定义 caption 分隔符与 tags-only metadata 数据集处理。
- 同步 vendored LyCORIS 至 `a72bb1b`，加入 weight-only FP8 旁路和新模型模块匹配。
- 引入固定版本的 `musubi-tuner` 作为 Krea 2 训练核心，上游源码保持在 `vendor/` 边界内。

### 升级提示

- 首次从旧版本启动时，CUDA 与 PyTorch 依赖迁移可能需要较长时间，请等待启动器完成处理。
- CUDA 13.0 需要 NVIDIA R580 或更高版本驱动。
- 项目要求 64 位 Python 3.10 至 3.12，推荐 Python 3.12。
- 如果现有 `venv` 由 Python 3.13 或 3.14 创建，请仅删除或重命名项目内的 `venv`，再重新运行启动器。
- Krea 2 训练前必须先完成潜空间缓存和 Qwen3-VL 文本编码器缓存；界面会在缓存缺失或过期时阻止启动。

### 回归保护

本版本补充了多训练核心、Krea 2 配置与缓存、时间步预览、内置文档、启动器、共享运行时、参数契约和实时状态相关的回归测试，覆盖本次架构升级的主要行为边界。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.3.3...v2.0.0)

## v1.3.3 - 2026-07-19

统一训练器实时通信并改善慢速远程连接体验，同时增强训练输出定位和标签编辑器的可靠性与响应式操作体验。

### 实时通信与慢速连接

- 使用同源 `/ws/realtime` 统一传递任务状态、训练进度、日志增量和硬件数据，补充后端实例识别、快照恢复与断线重连。
- 明确区分“实时数据延迟”和“后端断开”，后端重启后清理旧实例状态，避免将过期任务与监控数据误认为当前状态。
- 默认启用慢速连接兼容：完整展示样本列表，以低优先级单请求队列加载缩略图，并在实时数据延迟时暂停后台图片请求。
- 对预览图使用版本化缓存地址，优化监控标签页布局与历史记录加载，避免图片传输挤占关键实时信息。

### 训练输出与标签编辑器

- 在每个训练运行目录生成双语 `output_dir.txt`，记录模型、断点、训练状态和预览图的实际保存位置，并在删除历史时同步清理。
- 标签编辑器文本模式改为立即更新内存状态并防抖记录历史，保存、切图、撤销和离开页面前会结算待处理修改。
- 修复空标签草稿恢复、递归扫描与重新加载时的未保存保护，以及部分保存失败后脏状态和草稿的保留逻辑。
- 批量操作默认作用于已选图片，文件搜索和显示优先使用相对路径，并修复 `Ctrl+F`、输入框原生撤销及重命名按钮遮挡计数。
- 优化编辑面板宽度、图片预览高度、工具栏换行以及 1100px/900px 窄屏布局。

### 验证

- 新增实时通信、弱网加载、跨目录输出和标签编辑器契约测试。
- 标签编辑器使用 12 张图片、88 个唯一标签的数据集完成桌面与窄屏浏览器验证。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.3.2...v1.3.3)

## v1.3.2 - 2026-07-19

完成一轮严格保持现有功能和 API 契约不变的代码质量整理，降低后续维护成本，并为关键兼容行为补充回归保护。

### 服务端结构与任务维护

- 将原有单体 API 路由按 system、tagger、environment 拆分，保留 `backend.server.api.router` 兼容入口、全部 `/api/*` 路径和请求/响应结构。
- 抽取三类环境后台任务共用的完成任务 TTL 清理逻辑，保留安装日志删除回调与原有 600 秒清理时机。
- 抽取标注器共用的延迟 ONNX Session 创建逻辑，继续保持 Torch CUDA 库加载、CUDA/CPU provider 顺序、SessionOptions 日志级别和异常传播语义。

### 无效代码清理与验证

- 删除被最终 mixin 覆盖的前端定义、无引用私有成员、无引用局部导入及纯注释旧代码；保留当前实际生效的条件判断与停止训练交互。
- 新增 API 路由、任务清理、ONNX helper 和前端 mixin 源码契约测试，锁定无行为变化的边界。
- 全量 93 项单元测试、Python 编译检查和配置回退一致性检查通过。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.3.1...v1.3.2)

## v1.3.1 - 2026-07-18

修复 v1.3.0 Windows 启动器接管 GUI 输出后 Rich 日志失去颜色的问题，并将根目录的 Python 自动启动钩子移入内部工具目录。

### 控制台颜色与退出码

- GUI 进程不再经过 PowerShell 的 `Out-Host` 管道，Rich 可以重新识别交互式终端，恢复时间、日志级别和消息的彩色显示。
- 启动器改用独立状态保存 GUI 退出码，保持正常退出、错误返回以及 ZIP 修复后返回代码 23 自动重启的原有语义。
- 增加真实 PTY 冒烟验证和正常 ZIP 修复入口测试，确认彩色 ANSI 输出、参数转发与重启返回码均正常。

### 内部 Python 启动钩子

- 将根目录 `sitecustomize.py` 移至 `tools/python_startup/`，避免新用户误点内部兼容文件。
- Windows、Linux 启动器和训练子进程统一注入内部启动钩子目录；直接运行后端模块时也会显式加载。
- 保留 bitsandbytes 在 Windows 中文代码页下的兼容修复，并扩展自动加载与子进程编码测试。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.3.0...v1.3.1)

## v1.3.0 - 2026-07-18

重新设计 Windows 首次启动与安装流程，让直接从 GitHub 下载 ZIP 的新用户也能自动准备运行环境，并将目录安全转换为以后可使用 `git pull` 更新的仓库。

### Windows 首次启动与安装

- 将 `start.bat` 精简为兼容 Windows PowerShell 5.1 的入口，由新的 PowerShell 引导统一处理环境检测、安装、仓库修复和 GUI 启动。
- 自动复用 64 位 Python 3.10-3.12；缺少兼容版本时可为当前用户安装官方 Python 3.12.10，不替换新版 Python，也不修改默认 Python。
- Git 优先通过 winget 安装，失败时回退到固定版本的官方 Git for Windows 安装包，并校验 SHA-256 与 Authenticode 签名。
- 下载阶段显示百分比、大小、实时速度和 ETA；静默安装与创建 `venv` 等阶段显示加载动画和耗时，pip 与 Git 保留原生进度。
- 安装过程统一使用 English / 中文双语提示，并配置当前用户 PATH、Git Bash Here 及必要的资源管理器右键菜单。

### ZIP 仓库修复与数据保护

- 检测没有 `.git` 的 GitHub ZIP 目录，拉取完整 `main` 与标签，并在源码对齐前将差异文件、远端提交号和清单保存到 `bootstrap-backups/<时间>.zip`。
- 修复后创建跟踪 `origin/main` 的本地 `main`，设置 `pull.ff=only`，启动器自动重启一次；后续仍由用户手动执行 `git pull`。
- 源码对齐排除 `venv`、模型、缓存、输出、日志、Hugging Face 数据和整个 `config` 目录，不执行 `git clean` 或硬重置用户目录。
- 已有有效仓库不会被重设；损坏或来源无法确认的 `.git` 只显示双语警告。Git 安装或修复失败不会阻止训练器继续启动。

### 参数、Linux 与测试

- `--quiet/-q` 保持核心依赖自动安装并默认跳过可选 Git 修改；新增 `--setup-git` 与 `--skip-git-setup` 供非交互或显式跳过场景使用。
- Linux 启动器补齐 Python、Git 和 ZIP 下载版的中英双语说明，不自动调用发行版包管理器或 `sudo`。
- 新增 Windows 契约与临时远端集成测试，覆盖中文/空格路径、源码备份、用户数据保护、损坏仓库、下载失败及修复后的普通 `git pull`。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.2.0...v1.3.0)

## v1.2.0 - 2026-07-16

完整支持将训练产物保存到训练器目录外的任意位置，包括同盘跨目录和不同盘符；每条训练可独立选择输出目录，同时保持 TensorBoard、预览、日志和历史监控稳定可用。

### 自定义输出目录

- 模型、训练断点和采样预览图写入用户指定目录，配置、终端日志、TensorBoard 数据、训练结果与任务映射保存在训练器内部。
- 每条训练可使用不同输出目录，同盘跨目录与跨盘路径统一处理，不再依赖训练器目录下的相对路径。
- 启动前检查目录可用性和写入权限；默认目录不增加提示，自定义目录仅显示一行必要状态，不可用时阻止启动。

### 监控、历史与兼容

- 监控页通过任务映射读取外部产物，采样预览、文件列表、下载和最低 Loss checkpoint 识别均可正常使用。
- TensorBoard 统一读取训练器内部日志目录，即使不同任务使用不同产物目录，或外部目录暂时不可用，历史曲线仍可查看。
- 自动兼容并导入旧的跨目录训练记录；删除历史只清理内部日志和监控数据，不删除用户保存的训练产物。
- 增加任务路径映射与路径穿越校验，避免外部路径造成错误访问。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.1.3...v1.2.0)

## v1.1.3 - 2026-07-16

改善 Windows 首次安装与启动体验，自动处理不兼容的 Python 版本和 Microsoft Store 占位符，降低新用户误操作入口文件的概率。

### Python 安装与环境选择

- Windows 启动器优先复用兼容的项目 `venv`，否则依次查找 64 位 Python 3.12、3.11 和 3.10，并跳过 Microsoft Store 的 Python 占位符。
- 当电脑只有 Python 3.13/3.14 时，可为当前用户并行安装官方 Python 3.12，不卸载已有版本，也不修改系统默认 PATH。
- 自动下载 Python 3.12 时校验 Python Software Foundation 数字签名，失败时停止执行并提供手动下载地址。
- Linux 启动器同步限制受支持的 Python 版本，并在已有 `venv` 不兼容时给出明确的恢复方法。

### 启动入口与文档

- 将根目录的 `gui.py` 移入 `backend/gui.py`，启动脚本统一通过内部模块启动，避免新用户误点 Python 源文件绕过环境准备。
- 直接使用不兼容解释器启动内部模块时立即显示友好提示，不再进入依赖修复后因 wheel 不兼容而失败。
- 更新中英文必要依赖说明，明确 Python、Git、自动安装的 PyTorch/CUDA，以及 Windows 与 Linux 的环境处理差异。
- 修复训练页滚动条与相邻控件命中区域重叠的问题。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.1.2...v1.1.3)

## v1.1.2 - 2026-07-15

优化深浅主题的视觉层级与操作反馈，在保持中性灰承载面的同时，让文字、分组色和状态色恢复清晰鲜明。

### 主题与视觉层级

- 深色主题改为更舒适的中性灰层级，重新平衡页面背景、侧栏、卡片、输入控件和浮层之间的明度差异。
- 保持灰色集中在背景与承载层，恢复正文、参数分组、状态提示和代码高亮的鲜明色彩，避免界面整体灰蒙。
- 同步微调浅色主题的表面层级与边界表现，使两套主题在信息密度和视觉逻辑上保持一致。

### 开关与通知反馈

- 重做全局开关的尺寸、轨道、圆点和状态反馈，补充悬停、按压、键盘焦点及禁用状态，并保持克制顺滑的动效。
- 通知改用中性表面、彩色图标、轻量状态底色和低对比度整圈边框，移除左侧色条及大面积高亮状态色。
- 补齐警告通知，支持多行换行、长文本、堆叠与窄屏布局，并按错误、警告和普通消息调整停留时间。
- 通知进出改为短距离淡入淡出，取消弹跳缩放，并继续遵循 reduced-motion 设置。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.1.1...v1.1.2)

## v1.1.1 - 2026-07-13

训练监控体验升级为高信息密度的专业控制台，并修复历史日志、输出文件与实时刷新中的多项交互问题。

### 训练监控控制台

- 新增粘性任务控制条、紧凑硬件指标、响应式标签页与 12 栏概览布局，在首屏集中展示训练状态、进度、关键指标和样本。
- 将重复的 Loss 大图替换为规则驱动的训练诊断，展示趋势变化、波动系数、最低观测 Loss、判定依据和具体统计窗口。
- 增加默认折叠的算法依据与适用边界，公开数据源、窗口规则和判定阈值，并明确诊断不等同于成图质量、过拟合或最佳 checkpoint 判断。
- SSE 更新改为轻量局部刷新，重连时保留已有 Loss 数据，语言切换后立即重绘监控内容。

### 日志、样本与输出文件

- 修复历史记录初次打开时日志数量不正确、完整日志“顶部”按钮首屏不可用及回车覆盖日志被拆成多行的问题。
- 保留日志增量追加、分页、搜索、复制下载和滚动位置，并重新组织粘性工具栏操作层级。
- 样本预览改为不裁切布局，保留渐进加载和键盘灯箱导航；输出文件改为对齐表格和批量选择操作。
- 最低 Loss checkpoint 始终突出显示，Loss 相同时优先选择较新的存档。

### 视觉与可访问性

- 全站面板统一为直角，按钮和输入控件收紧为轻微直角，移除监控页多余渐变、阴影与高饱和提示气泡。
- 增加标准 tab 语义、键盘切换、焦点样式、连接状态文本和 reduced-motion 支持。
- 完善中英文监控文案、空闲状态说明、历史数据标识及训练诊断解释。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.1.0...v1.1.1)

## v1.1.0 - 2026-07-13

PC 端界面与交互体验升级，在保持原有布局和 LoRA 参数彩色分组的基础上，提高信息密度、状态可读性和页面响应速度。

### 界面与状态重设计

- 统一卡片、表单、按钮、下拉框和侧栏的边界、圆角与层级，减少过度圆弧、阴影和装饰感，使界面更贴近桌面生产力工具。
- 保留 LoRA 参数分组彩色识别，并将侧栏选中标记收窄为短竖线，兼顾快速定位与视觉克制。
- 重做环境状态、模型条目和下载操作的展示方式，降低高饱和状态色的刺激感并修正内容对齐。
- 精简日志工具栏，移除低频的“跳到第 N 行”控件，为搜索、翻页和复制操作释放横向空间。
- 统一训练类型与普通下拉框的交互和指示线宽，并优化训练步数区域的标签、数值和公式对齐。

### 页面切换与动画性能

- 新增轻量页面切换进度反馈，首次进入训练页时先完成视觉响应，再挂载较重的训练表单。
- 缓存训练表单 DOM 与 Alpine 状态，离开页面时仅暂停轮询，返回时直接复用，减少重复初始化造成的卡顿。
- 参数条件显隐改为批量 FLIP 动画，只触发一次布局切换，并使用更自然的非线性缓动表现出现、收起和周围字段补位。
- 为后台标签页、动画中断、快速反向切换和减少动态效果偏好增加清理与兜底，避免残留动画状态和无效资源占用。
- 减少下拉菜单常驻 DOM 与重复窗口尺寸读取，降低非活动界面的渲染和监听开销。

### 训练步数与本地化

- 训练步数预估接口返回结构化错误代码与参数，由前端按当前语言显示可读提示。
- 补充数据集、分辨率、GPU、分桶和图片读取等错误的中英文文案，避免界面直接显示混合语言后端错误。
- 修正训练启动前步数校验的错误文本来源，并增加本地化错误上下文与前端合同回归测试。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.0.2...v1.1.0)

## v1.0.2 - 2026-07-12

训练步数预估与界面体验更新，帮助用户在训练开始前直观确认实际训练规模。

### 训练步数计算

- 新增训练步数计算区域，展示原图数、目录重复次数、Batch、梯度累积、Epoch、GPU 数与预计总步数。
- 以易读公式逐步说明训练样本、每轮批次数、每轮优化步数及最终总步数的计算过程。
- 复用 sd-scripts 的图片扫描、分桶和向上取整规则，使预估结果与实际训练保持一致。
- 支持切换训练集目录及修改相关训练参数后自动重新计算，并提供手动刷新入口。
- 开始训练前强制重新扫描与校验，避免使用过期的数据集统计结果。

### 界面优化

- 重算期间保留上一次结果，固定计算区域高度，避免下方训练选项上下移动。
- 增加理论等效 Batch 展示，明确 Batch、梯度累积与多 GPU 数量之间的关系。
- 保留梯度累积和梯度检查点原有字段说明，避免将两个独立参数误解为自动联动。
- 新增后端计算、sd-scripts 分桶对照及前端刷新行为回归测试。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.0.1...v1.0.2)

## v1.0.1 - 2026-07-11

稳定性与体验优化版本，重点改善大型数据集下的标签编辑效率和训练监控状态管理。

### 标签编辑器优化

- 合并图片列表与标签频率扫描，减少大型数据集的重复磁盘遍历。
- 新增 320px 列表缩略图和 960px 预览缓存，缩短图片加载时间。
- 批量保存、批量编辑和预览操作移至后台线程，避免阻塞服务响应。
- 历史记录改用增量变更，降低连续编辑时的内存占用。
- 修复全局标签重命名的撤销与重做行为，并完善历史详情显示。
- 增加选择当前页、选择全部筛选结果及窄屏纵向布局。
- 改进弹窗语义、自动焦点和图片预览交互。

### 稳定性修复

- 修复退出历史任务详情后，训练监控仍残留历史日志的问题。
- 修复单图保存后递归数据集缓存仍返回旧标签的问题。
- 修复标签频率请求失败后旧索引残留导致统计不准确的问题。
- 为缩略图缓存增加自动清理上限，防止长期使用持续占用磁盘。
- 新增标签编辑器后端与前端合同回归测试。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.0.0...v1.0.1)

## v1.0.0 - 2026-07-11

首个正式稳定版本，提供完整的 Anima 与 SDXL LoRA 本地训练工作流。

### 主要功能

- 基于 FastAPI 与 Alpine.js 的本地训练工作台，集成 `sd-scripts` 训练引擎。
- 支持 Anima（Qwen3 + T5 双编码器）和 SDXL LoRA 训练。
- 提供训练参数表单、TOML 预览、预设管理与严格的模型相关参数校验。
- 提供实时硬件监控、训练日志、历史记录、Loss 统计与预览图灯箱。
- 内置标签编辑器、WD14 自动打标、模型下载与训练环境管理。
- 支持中英文界面、亮暗主题和 Windows/Linux 启动脚本。

### 正式版改进

- 修复训练样本预览串样本、扫描卡顿及多行采样提示词拼接问题。
- 优化训练日志查看体验和训练任务并发槽位管理。
- 对齐 `sd-scripts` 的字段范围、模型分组与 Anima/SDXL 分辨率约束。
- 完善 Anima 模型、VAE、Qwen3、dropout、token 与时间步参数校验。
- 优化图片计数、预览扫描和训练启动路径的性能。
- 精简重复字段提示，并同步 API 与前端离线回退配置。

[完整变更](https://github.com/amenorira/lora-scripts-anima/compare/v1.0.0-rc.3...v1.0.0)
